spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/own_app?useUnicode=true&characterEncoding=utf-8&serverTimezone=UTC
    username: root
    password: admin

mybatis:
  mapper-locations: classpath*:mapper/*Mapper.xml
  type-aliases-package: com.fuchuang.race.model
